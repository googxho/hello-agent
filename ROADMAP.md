# 学习路线图

> **一个知识块 = 一个版本。** 每版只引入一个新概念。
>
> ⚠️ **每完成一版，必须回来更新这张表**（见 [AGENTS.md](AGENTS.md) 铁律 3）。
> 这张表是你的**全图景**，不能让它过期。
>
> 💡 **想知道「外面有什么现成方案」？** → [ECOSYSTEM.md](ECOSYSTEM.md)
> 社区方案速览：按知识块对照（压缩 / 记忆 / RAG / 评估 / 框架……），
> 讲清**每一类解法的思路**，而不是罗列产品名。
> 建议学完某一版之后去翻对应那一节 —— 你会发现你走的路和它们是一样的。

---

## 当前进度

| | |
|---|---|
| ✅ 已完成 | **4** 个知识块（v1、v2、v3、v4） |
| ⬅️ 下一版 | **v5 · 本地检索（RAG）** |
| ⬜ 剩余 | **10** 个知识块 |

---

## 总览

| # | 知识块 | 解决什么问题 | 状态 | 版本 |
|---|---|---|---|---|
| 01 | 最小闭环 | 「Agent 究竟是什么？」 | ✅ | v1 |
| 02 | 上下文管理 | 聊久了会爆窗口、会烧钱 | ✅ | v2 |
| 03 | 上下文压缩 | 剪掉就真没了，它忘得很安静 | ✅ | v3 |
| 04 | 持久化记忆 | 重启就全忘光 | ✅ | v4 |
| 05 | **本地检索（RAG）** | 它不知道我的资料 | ⬅️ **下一版** | |
| 06 | 结构化输出 | 输出格式不可控，没法程序化消费 | ⬜ | |
| 07 | 评估与可观测 | 改了 prompt，不知道是变好还是变坏 | ⬜ | |
| 08 | 规划与任务拆解 | 复杂任务它只会走一步看一步 | ⬜ | |
| 09 | 反思与自我修正 | 错了不会自我批评 | ⬜ | |
| 10 | 流式与人机协同 | 等半天没反应，中途没法干预 | ⬜ | |
| 11 | 多 Agent 协作 | 一个 Agent 兼太多角色 | ⬜ | |
| 12 | MCP 工具生态 | 每个工具都要手写 | ⬜ | |
| 13 | 框架化（LangChain / LangGraph） | 手写开始失控 | ⬜ | |
| 14 | 成本、并发与部署 | 贵、慢、上不了线 | ⬜ | |

---

## 为什么是这个顺序

路线不是随便排的，它是一条**逐步加难**的路径，分四个阶段：

| 阶段 | 块 | 主题 | 一句话 |
|---|---|---|---|
| 一 | 01 – 02 | **能跑起来** | 先有个 Agent，再让它别忘事 |
| 二 | 03 – 05 | **记得住** | 压缩 → 持久化 → 检索，一步步扩大记忆 |
| 三 | 06 – 09 | **靠得住** | 输出可控、能评估、会规划、会反思 |
| 四 | 10 – 14 | **上工程** | 交互、协作、生态、框架、成本 |

**核心思路：先让它活下来，再让它记住，再让它可靠，最后才谈规模。**

很多人反着来 —— 一上来就搭多 Agent 框架，结果连「工具描述写不好」这种
基础问题都没解决，调都不知道从哪调。

---

# 详细说明

## ✅ 01 · 最小闭环（v1）

**解决什么问题**：Agent 究竟是什么？

**核心概念**：`Agent = LLM + 工具 + 循环 + 状态`

**学到了什么**
- LLM 只是个「文本进、文本出」的函数：不会算术、不知道今天几号、改不了磁盘
- Agent 框架做的事：给它配上手（工具），让它在循环里反复调用自己
- 循环的终止条件就是「这一轮没有 tool_calls」—— 由模型自己决定何时收工
- **工具描述就是写给模型的提示词**（第 10 个实验亲手验证过）
- `role="tool"` 消息必须带 `tool_call_id`，否则模型不知道这是回应哪次调用

**留下了什么坑**
- `messages` 只增不减 → 聊久了必爆窗口
- 完全没有 token 可观测性
- `MAX_ROUNDS` 只保护「单次提问内部」的循环，管不了跨轮次累积
- 单文件 400 行，加东西越来越难

---

## ✅ 02 · 上下文管理（v2）

**解决什么问题**：v1 的 messages 只增不减，token 线性增长，最终必然撞上下文窗口。

**核心概念**：给上下文设 token 预算，超预算时优雅地「忘记」最不重要的部分。

**学到了什么**
- ⭐ **替换 content，而不是删除消息** —— 删 `role="tool"` 消息会破坏
  `tool_call_id` 配对，API 直接报错
- 两级剪枝：先剪工具结果（伤害小），再按整轮丢弃旧对话（伤害大）
- **look-ahead 算账**：如果预判「光剪工具结果不够」，就直接丢整轮，
  否则剪完又被丢掉，白忙一场
- 两条护栏：当前轮永不动、工具结果超长源头截断
- 顶到保护区时要**如实标记** `hit_floor`，不能假装成功
- 估算值（启发式）与真实值（API `usage`）要并排显示，才知道误差多大

**留下了什么坑**
- ⭐ **剪枝是不可逆的，剪掉就真没了** —— 占位符里没有任何信息
- 越活跃的会话越早失忆，但恰恰是活跃会话最需要早前的信息
- 重启就全忘光，进程之间不共享
- 只会用「手上有」的工具，没有检索能力

---

## ✅ 03 · 上下文压缩（v3）

**解决什么问题**

v2 的剪枝是**丢弃**。实测：让它记住「紫色河马」，聊 8 轮无关的，再回头问 ——
它答不上来，而且**没有任何报错**。暗号不是变模糊，是彻底不存在了。

**核心概念**

**用「压缩」替代「丢弃」**：即将被剪掉的那批消息，先交给 LLM 概括成一条摘要，
摘要替换掉原文，用少量 token 保住关键信息。顺带引出**记忆分层**：
短期上下文（原文）vs 长期摘要（压缩后）。

**学到了什么**

- ⭐ **摘要替换原文，不是删除** —— 上下文从此有了两层：一条 `system` 摘要（长期）
  + 最近的原文（短期）。「上下文变短了」是表象，「按重要性分了档」才是本质
- **摘要必须结构化**：把「必须保留」的类别列死（目标 / 关键信息 / 已确认的事实 / 未完成），
  远胜于「请概括一下」—— 后者会写出一段读起来很顺、但把暗号丢掉的文章
- ⭐ **滚动合并**：压第二次时，旧摘要要和新内容**一起**送进去，输出合并后的完整摘要。
  少了这一步，每次压缩都是一次「换方式的失忆」
- **摘要用 `system` 承载**（固定插在 index 1）：`user` 会被 `_drop_oldest_rounds`
  当成轮次起点误删，`system` 不会
- ⭐ **压缩不省钱**：实测压缩一次花 789 tokens（608 输入 + 181 输出），只省下约 150。
  它买的是「信息存续」，不是「省 token」—— 省下的 token 每轮都在兑现，而丢弃只兑现一次
- **降级路径是工程底线**：压缩失败（网络 / 模型名写错 / 配额）必须回退到 v2 的丢弃。
  5 行 `except`，决定这个 Agent 能不能上生产
- ⭐ **开关写着「开」，不等于它真的生效**：`MIN_COMPACT_TOKENS` 设成 400 时，
  预算小一点的配置下压缩**永不触发**，v3 悄悄退回 v2 —— 只有日志里的 emoji 能告诉你真相
- **可测性靠依赖注入**：把 `Summarizer` 抽成 Protocol，测试里塞一个假的，
  整条压缩流水线就能离线测穿（40 项断言，零 API Key）
- **对照组测试法**：同一个测试里一边断言「新行为对」（压缩后暗号还在）、
  一边断言「旧行为错」（丢弃后暗号没了）—— 以后谁把它改退化，测试立刻响

**留下了什么坑**

- ⭐ **摘要只活在进程内存里**，重启就全没了；会话之间不共享任何东西
- **压了又压会漂移**：每次压缩都是有损转换，第三次压完，第一次的细节已经很淡
- **预算太小会压不动**：摘要 + 最近保护区本身就快撑满预算，没有可压的空间（`hit_floor`）
- **估算器不含 tools schema**：那几个工具的 JSON Schema 是每次请求的固定开销（约 400 tokens），
  没被算进去 —— 估算 501 时，真实请求已经 1,002
- **压缩是同步阻塞的**：实测一次 1.2 秒，对话会明显卡一下
- 只会用「手上有」的工具，没有检索能力

---

## ✅ 04 · 持久化记忆（v4）

**解决什么问题**

v3 的记忆有保质期，但保质期的单位是**进程**：`messages` 和 `summary_text`
全躺在 Python 变量里，关掉终端就蒸发。

实测对照（同一个暗号：第一次启动埋下、退出，第二次启动问它）：

- v3：「如果你之前是在**别的会话**里设定的暗号，我这边看不到」❌
- v4：**「暗号是『紫色河马』。」** ✅

**核心概念**

把会话（消息 + 摘要）写进磁盘，启动时读回来。

```
内存里的状态 ──写─→ session.json ──读─→ 内存里的状态
```

**学到了什么**

- ⭐ **原子写**：写 `.tmp` → `flush`+`fsync` → `os.replace()`。
  不是洁癖 —— 「写到一半被 kill」是真的会发生，而半截 JSON 等于整场会话报废
- **`fsync` 不能省**：少了它，数据可能还在系统缓存里 ——
  断电之后，你「原子替换」保住的其实是个空文件
- ⭐ **容错读**：读盘的任何异常都不上抛，而是降级成四种明确状态
  （`restored` / `missing` / `corrupt` / `future_version`），每种都带一句人话说明
- **坏文件是改名、不是删除**：里面可能是用户聊了一下午的内容，
  程序没有资格替人做主扔掉它
- **版本号从第一天就要有** —— 等你需要它的时候再加，老文件里就没有它了
- ⭐ **两份状态一定会打架**：`/reset` 必须同时清内存和磁盘，并如实汇报做没做到
  （只清内存 → 下一轮存盘或重启后，它会「全想起来」）
- **存「会话」不存「提示词」**：提示词属于代码 —— 这样以后改了提示词，老会话也能用上
- **结构校验要放在启动那一刻**：坏结构如果放进去，
  会在第一次请求模型时才炸 —— 那时你早忘了数据是从磁盘读来的
- **触发点放 CLI 层**：什么时候存盘是**产品决策**，怎么存才是技术问题。
  `agent.py` 这一版一行没改
- 实测：写盘 1.3 ms / 308 B —— 便宜到可以「每轮都存」，不必赌「用户会正常退出」

**留下了什么坑**

- ⭐ **一个文件 = 一场会话**：没有列表、没有命名、没有切换
  （`cp` + `LLM_SESSION_FILE=` 就是全部机制）
- **只记得「这场会话」**：跨会话的偏好（我是谁、我喜欢什么）还是记不住
- ⭐ **它只记得「你说过的话」**，不记得「你的资料」——
  问它笔记里的内容，它一无所知。**它不是不记得，是从来没看过**
- 没有并发保护：两个进程同时打开同一个会话文件，后写的会覆盖先写的
- `.corrupt` 备份会一直堆着，没有清理策略

---

## ⬅️ 05 · 本地检索（RAG）（下一版 · v5）

**解决什么问题**

v4 让 Agent 记住了「你告诉过它的事」，但它对**你已经写在磁盘上的东西**一无所知：
你的笔记（`notes/`）、文档、代码库。

你问「我上次记的那个项目叫什么」，它只能说没听过 —— 尽管答案就躺在
`agent-v4/notes/` 里，离它只有几厘米。

**核心概念**

**embedding + 切分 + 召回**：把资料切块、转成向量存起来；
提问时先检索出最相关的几块，塞进上下文，再让它回答。

**起点**（动手前先想清楚）

1. 先跑一下：「读一下 notes 目录里的笔记」—— 感受 v4 的天花板（它连列目录的工具都没有）
2. 回顾 v4 的 `storage.py`：它已经示范了「怎么和外部数据打交道」（原子写、容错读）
3. 回顾 v2/v3 的预算与压缩：**召回的内容要占上下文** —— 又把前面几版接回来了

**预计会撞的墙**

| 墙 | 说明 |
|---|---|
| **切分粒度** | 一句话？一段？固定 500 字？切得太碎丢上下文，切得太大召回不准 |
| **怎么找** | 关键词匹配（简单、离线）vs embedding（要调 API / 新依赖）。第一次上 RAG，先试哪种？ |
| **召回多少** | 召回 3 条可能漏，召回 20 条直接把上下文塞满 —— 又撞上 v2 的预算 |
| **索引放哪** | 每次启动重建（慢）还是存盘（要管新鲜度）—— v4 的 storage 层可以借鉴 |
| **它得先知道该查** | 模型不会自动去查资料 —— 要写成工具 + 描述，还是写进系统提示词？ |
| **相关性判断** | 召回了 5 条，其中 3 条没用 —— 模型会不会被带偏？ |

**验证思路**

- **离线**：造一个小知识库（几个 `notes/*.md`）→ 断言「问 X 时，召回的正是 X 那一段」（不需要 API Key）
- **对比**：同一段资料、同一个问题，不接检索 vs 接检索 —— 和 v3/v4 一样的手法
- **量化**：召回了几条、占了多少 token、答案有没有引用到资料
- **反例**：故意问一个资料里**没有**的东西，看它会不会硬编一个答案

**做完之后，这张表要更新成**：05 标 ✅，06（结构化输出）变成下一版。

---

## ⬜ 05 · 本地检索（RAG）

**解决什么问题**：Agent 不知道「你的资料」—— 你的文档、笔记、代码库。

**核心概念**：embedding + 向量库 + 切分 + 召回 + 重排。

**预计会撞的墙**：切分粒度怎么选？召回不准怎么办？召回的内容塞进上下文，
又回到了 v2/v3 的预算问题 —— 这时你会发现前面几版的价值。

---

## ⬜ 06 · 结构化输出

**解决什么问题**：模型的输出是自由文本，程序没法可靠消费。

**核心概念**：JSON Schema 约束、校验、失败重试。

**预计会撞的墙**：模型不遵守 schema 怎么办？嵌套结构？流式下怎么解析半截 JSON？

---

## ⬜ 07 · 评估与可观测

**解决什么问题**：改了 prompt 或工具描述，**你不知道是变好了还是变坏了**。

**核心概念**：trace（每一步都记下来）+ eval 集（一批固定测试用例 + 期望结果）。

**预计会撞的墙**：LLM 输出没有唯一正确答案，怎么判定「对」？
这是最容易被跳过、但**回报最高**的一块 —— 没有它，前面所有优化都是瞎猜。

---

## ⬜ 08 · 规划与任务拆解

**解决什么问题**：复杂任务，它只会「走一步看一步」，容易迷路。

**核心概念**：Plan-and-Execute（先出计划再执行）、ReAct（边想边做）、任务分解。

**预计会撞的墙**：计划做出来但执行偏了怎么办？计划要不要给用户确认？

---

## ⬜ 09 · 反思与自我修正

**解决什么问题**：它做错了不会自我批评，错了就一直错下去。

**核心概念**：Reflexion —— 让模型检查自己的输出并改进。

**预计要撞的墙**：自我批评经常是无效的（它觉得自己对）；反思也要花 token。

---

## ⬜ 10 · 流式与人机协同

**解决什么问题**：等半天没反应；中途没法干预。

**核心概念**：streaming 输出、中断、human-in-the-loop（危险操作前要确认）。

**预计会撞的墙**：流式下 `tool_calls` 是分片到达的，要自己拼装；
流式和剪枝的交互也很微妙。

---

## ⬜ 11 · 多 Agent 协作

**解决什么问题**：一个 Agent 兼太多角色（规划、执行、检查），提示词互相打架。

**核心概念**：角色分工、handoff（交接）、编排。

**⚠️ 注意**：这一块最容易被过度使用。**先确认单个 Agent 真的做不好**再上多 Agent。

---

## ⬜ 12 · MCP 工具生态

**解决什么问题**：每个工具都要手写，接不了外部生态。

**核心概念**：MCP（Model Context Protocol）—— 用标准协议接入别人写好的工具。

---

## ⬜ 13 · 框架化（LangChain / LangGraph）

**解决什么问题**：到这里，手写的主循环通常会开始失控。

**⚠️ 什么时候才该上框架 —— 看信号，别看版本号**

出现下面任意一条，就说明到时候了：

- 主循环里出现 **3 个以上 `if/else`** 在决定「接下来做什么」
- 想加「这条失败就换条路走」，发现要在循环里再嵌一层
- 多 Agent 之间的消息传递，开始需要**手写状态机**
- 想实现「暂停 → 等用户确认 → 继续」，发现自己要管一大堆状态

**这时 LangGraph 的价值才会显现** —— 它给的就是「状态机 + 检查点 + 人机协同」。

> **在没痛过之前上框架，你只会觉得它又重又绕。**
> 这也是为什么它排在最后几块，而不是第一块。

---

## ⬜ 14 · 成本、并发与部署

**解决什么问题**：贵、慢、上不了线。

**核心概念**：prompt 缓存、并发、限流、重试、成本监控、部署形态。

---

# 速查：每块对应哪个「痛」

| 你感受到的痛 | 该学哪块 |
|---|---|
| 「它究竟是什么？」 | 01 |
| 「怎么越聊越贵，还会报错」 | 02 |
| 「它怎么把我刚说的事忘了」 | 03 |
| 「重启就什么都不记得」 | 04 |
| 「它不知道我的资料」 | 05 |
| 「输出没法程序化消费」 | 06 |
| 「改了 prompt 不知道有没有变好」 | 07 |
| 「复杂任务它做不明白」 | 08 |
| 「错了不会自己改」 | 09 |
| 「等半天没反应」 | 10 |
| 「一个提示词里塞太多角色」 | 11 |
| 「工具全要手写」 | 12 |
| 「主循环快改成意大利面了」 | 13 |
| 「太贵/太慢/上不了线」 | 14 |
