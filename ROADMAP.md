# 学习路线图

> **一个知识块 = 一个版本。** 每版只引入一个新概念。
>
> ⚠️ **每完成一版，必须回来更新这张表**（见 [AGENTS.md](AGENTS.md) 铁律 3）。
> 这张表是你的**全图景**，不能让它过期。
>
> 💡 **想知道「外面有什么现成方案」？** → [ECOSYSTEM.md](ECOSYSTEM.md)
> 社区方案速览：按知识块对照（压缩 / 记忆 / RAG / 评估 / 框架……），
> 讲清**每一类解法的思路**，而不是罗列产品名。
> 建议学完某一版之后去翻对应那一节 —— 你会发现你走的路和它们是一样的。

---

## 当前进度

| | |
|---|---|
| ✅ 已完成 | **7** 个知识块（v1 ~ v7） |
| ⬅️ 下一版 | **v8 · 结构化输出** |
| ⬜ 剩余 | **9** 个知识块 |

---

## 总览

| # | 知识块 | 解决什么问题 | 状态 | 版本 |
|---|---|---|---|---|
| 01 | 最小闭环 | 「Agent 究竟是什么？」 | ✅ | v1 |
| 02 | 上下文管理 | 聊久了会爆窗口、会烧钱 | ✅ | v2 |
| 03 | 上下文压缩 | 剪掉就真没了，它忘得很安静 | ✅ | v3 |
| 04 | 持久化记忆 | 重启就全忘光 | ✅ | v4 |
| 05 | 本地检索（RAG）· 基础版 | 它不知道我的资料 | ✅ | v5 |
| 05b | 语义检索（embedding）| 换个说法就搜不到了 | ✅ | v6 |
| 05c | 混合检索（关键词 + 向量）| 两种各有强项，为何只选一个 | ✅ | v7 |
| 06 | **结构化输出** | 输出格式不可控，没法程序化消费 | ⬅️ **下一版** | |
| 07 | 评估与可观测 | 改了 prompt，不知道是变好还是变坏 | ⬜ | |
| 08 | 规划与任务拆解 | 复杂任务它只会走一步看一步 | ⬜ | |
| 09 | 反思与自我修正 | 错了不会自我批评 | ⬜ | |
| 10 | 流式与人机协同 | 等半天没反应，中途没法干预 | ⬜ | |
| 11 | 多 Agent 协作 | 一个 Agent 兼太多角色 | ⬜ | |
| 12 | MCP 工具生态 | 每个工具都要手写 | ⬜ | |
| 13 | 框架化（LangChain / LangGraph） | 手写开始失控 | ⬜ | |
| 14 | 成本、并发与部署 | 贵、慢、上不了线 | ⬜ | |

---

## 为什么是这个顺序

路线不是随便排的，它是一条**逐步加难**的路径，分四个阶段：

| 阶段 | 块 | 主题 | 一句话 |
|---|---|---|---|
| 一 | 01 – 02 | **能跑起来** | 先有个 Agent，再让它别忘事 |
| 二 | 03 – 05 | **记得住** | 压缩 → 持久化 → 检索，一步步扩大记忆 |
| 三 | 06 – 09 | **靠得住** | 输出可控、能评估、会规划、会反思 |
| 四 | 10 – 14 | **上工程** | 交互、协作、生态、框架、成本 |

**核心思路：先让它活下来，再让它记住，再让它可靠，最后才谈规模。**

很多人反着来 —— 一上来就搭多 Agent 框架，结果连「工具描述写不好」这种
基础问题都没解决，调都不知道从哪调。

---

# 详细说明

## ✅ 01 · 最小闭环（v1）

**解决什么问题**：Agent 究竟是什么？

**核心概念**：`Agent = LLM + 工具 + 循环 + 状态`

**学到了什么**
- LLM 只是个「文本进、文本出」的函数：不会算术、不知道今天几号、改不了磁盘
- Agent 框架做的事：给它配上手（工具），让它在循环里反复调用自己
- 循环的终止条件就是「这一轮没有 tool_calls」—— 由模型自己决定何时收工
- **工具描述就是写给模型的提示词**（第 10 个实验亲手验证过）
- `role="tool"` 消息必须带 `tool_call_id`，否则模型不知道这是回应哪次调用

**留下了什么坑**
- `messages` 只增不减 → 聊久了必爆窗口
- 完全没有 token 可观测性
- `MAX_ROUNDS` 只保护「单次提问内部」的循环，管不了跨轮次累积
- 单文件 400 行，加东西越来越难

---

## ✅ 02 · 上下文管理（v2）

**解决什么问题**：v1 的 messages 只增不减，token 线性增长，最终必然撞上下文窗口。

**核心概念**：给上下文设 token 预算，超预算时优雅地「忘记」最不重要的部分。

**学到了什么**
- ⭐ **替换 content，而不是删除消息** —— 删 `role="tool"` 消息会破坏
  `tool_call_id` 配对，API 直接报错
- 两级剪枝：先剪工具结果（伤害小），再按整轮丢弃旧对话（伤害大）
- **look-ahead 算账**：如果预判「光剪工具结果不够」，就直接丢整轮，
  否则剪完又被丢掉，白忙一场
- 两条护栏：当前轮永不动、工具结果超长源头截断
- 顶到保护区时要**如实标记** `hit_floor`，不能假装成功
- 估算值（启发式）与真实值（API `usage`）要并排显示，才知道误差多大

**留下了什么坑**
- ⭐ **剪枝是不可逆的，剪掉就真没了** —— 占位符里没有任何信息
- 越活跃的会话越早失忆，但恰恰是活跃会话最需要早前的信息
- 重启就全忘光，进程之间不共享
- 只会用「手上有」的工具，没有检索能力

---

## ✅ 03 · 上下文压缩（v3）

**解决什么问题**

v2 的剪枝是**丢弃**。实测：让它记住「紫色河马」，聊 8 轮无关的，再回头问 ——
它答不上来，而且**没有任何报错**。暗号不是变模糊，是彻底不存在了。

**核心概念**

**用「压缩」替代「丢弃」**：即将被剪掉的那批消息，先交给 LLM 概括成一条摘要，
摘要替换掉原文，用少量 token 保住关键信息。顺带引出**记忆分层**：
短期上下文（原文）vs 长期摘要（压缩后）。

**学到了什么**

- ⭐ **摘要替换原文，不是删除** —— 上下文从此有了两层：一条 `system` 摘要（长期）
  + 最近的原文（短期）。「上下文变短了」是表象，「按重要性分了档」才是本质
- **摘要必须结构化**：把「必须保留」的类别列死（目标 / 关键信息 / 已确认的事实 / 未完成），
  远胜于「请概括一下」—— 后者会写出一段读起来很顺、但把暗号丢掉的文章
- ⭐ **滚动合并**：压第二次时，旧摘要要和新内容**一起**送进去，输出合并后的完整摘要。
  少了这一步，每次压缩都是一次「换方式的失忆」
- **摘要用 `system` 承载**（固定插在 index 1）：`user` 会被 `_drop_oldest_rounds`
  当成轮次起点误删，`system` 不会
- ⭐ **压缩不省钱**：实测压缩一次花 789 tokens（608 输入 + 181 输出），只省下约 150。
  它买的是「信息存续」，不是「省 token」—— 省下的 token 每轮都在兑现，而丢弃只兑现一次
- **降级路径是工程底线**：压缩失败（网络 / 模型名写错 / 配额）必须回退到 v2 的丢弃。
  5 行 `except`，决定这个 Agent 能不能上生产
- ⭐ **开关写着「开」，不等于它真的生效**：`MIN_COMPACT_TOKENS` 设成 400 时，
  预算小一点的配置下压缩**永不触发**，v3 悄悄退回 v2 —— 只有日志里的 emoji 能告诉你真相
- **可测性靠依赖注入**：把 `Summarizer` 抽成 Protocol，测试里塞一个假的，
  整条压缩流水线就能离线测穿（40 项断言，零 API Key）
- **对照组测试法**：同一个测试里一边断言「新行为对」（压缩后暗号还在）、
  一边断言「旧行为错」（丢弃后暗号没了）—— 以后谁把它改退化，测试立刻响

**留下了什么坑**

- ⭐ **摘要只活在进程内存里**，重启就全没了；会话之间不共享任何东西
- **压了又压会漂移**：每次压缩都是有损转换，第三次压完，第一次的细节已经很淡
- **预算太小会压不动**：摘要 + 最近保护区本身就快撑满预算，没有可压的空间（`hit_floor`）
- **估算器不含 tools schema**：那几个工具的 JSON Schema 是每次请求的固定开销（约 400 tokens），
  没被算进去 —— 估算 501 时，真实请求已经 1,002
- **压缩是同步阻塞的**：实测一次 1.2 秒，对话会明显卡一下
- 只会用「手上有」的工具，没有检索能力

---

## ✅ 04 · 持久化记忆（v4）

**解决什么问题**

v3 的记忆有保质期，但保质期的单位是**进程**：`messages` 和 `summary_text`
全躺在 Python 变量里，关掉终端就蒸发。

实测对照（同一个暗号：第一次启动埋下、退出，第二次启动问它）：

- v3：「如果你之前是在**别的会话**里设定的暗号，我这边看不到」❌
- v4：**「暗号是『紫色河马』。」** ✅

**核心概念**

把会话（消息 + 摘要）写进磁盘，启动时读回来。

```
内存里的状态 ──写─→ session.json ──读─→ 内存里的状态
```

**学到了什么**

- ⭐ **原子写**：写 `.tmp` → `flush`+`fsync` → `os.replace()`。
  不是洁癖 —— 「写到一半被 kill」是真的会发生，而半截 JSON 等于整场会话报废
- **`fsync` 不能省**：少了它，数据可能还在系统缓存里 ——
  断电之后，你「原子替换」保住的其实是个空文件
- ⭐ **容错读**：读盘的任何异常都不上抛，而是降级成四种明确状态
  （`restored` / `missing` / `corrupt` / `future_version`），每种都带一句人话说明
- **坏文件是改名、不是删除**：里面可能是用户聊了一下午的内容，
  程序没有资格替人做主扔掉它
- **版本号从第一天就要有** —— 等你需要它的时候再加，老文件里就没有它了
- ⭐ **两份状态一定会打架**：`/reset` 必须同时清内存和磁盘，并如实汇报做没做到
  （只清内存 → 下一轮存盘或重启后，它会「全想起来」）
- **存「会话」不存「提示词」**：提示词属于代码 —— 这样以后改了提示词，老会话也能用上
- **结构校验要放在启动那一刻**：坏结构如果放进去，
  会在第一次请求模型时才炸 —— 那时你早忘了数据是从磁盘读来的
- **触发点放 CLI 层**：什么时候存盘是**产品决策**，怎么存才是技术问题。
  `agent.py` 这一版一行没改
- 实测：写盘 1.3 ms / 308 B —— 便宜到可以「每轮都存」，不必赌「用户会正常退出」

**留下了什么坑**

- ⭐ **一个文件 = 一场会话**：没有列表、没有命名、没有切换
  （`cp` + `LLM_SESSION_FILE=` 就是全部机制）
- **只记得「这场会话」**：跨会话的偏好（我是谁、我喜欢什么）还是记不住
- ⭐ **它只记得「你说过的话」**，不记得「你的资料」——
  问它笔记里的内容，它一无所知。**它不是不记得，是从来没看过**
- 没有并发保护：两个进程同时打开同一个会话文件，后写的会覆盖先写的
- `.corrupt` 备份会一直堆着，没有清理策略

---

## ✅ 05 · 本地检索（RAG）· 基础版（v5）

**解决什么问题**

v4 让 Agent 记住了「你告诉过它的事」，但它对**你已经写在磁盘上的资料**一无所知。

实测对照（同一份笔记 `chenxi.md`，就放在 `notes/` 里）：

- v4：「我**没有读取或检索已有笔记的能力**——所以我无法看到……」❌
- v5：「根据你的笔记（chenxi.md）：交付时间：本周五；负责人：老王」✅

**核心概念**

**先找出来，再塞进去**：`问题 → 检索器 → 最相关的 N 段 → 塞进上下文 → 回答`

三个必须做的决定：

| 决定 | 这一版的选择 | 为什么 |
|---|---|---|
| 怎么切 | 按 Markdown 标题 + 段落 | 段落是语义的自然边界 |
| 怎么找 | 关键词打分（IDF × 饱和词频）| 零依赖、零 API、**完全离线可测** |
| 中文怎么办 | 字符 bigram | 中文没有空格，装分词器要引入依赖 |

**学到了什么**

- ⭐ **检索就是一条五步流水线**：切分 → 分词 → 索引 → 打分 → 塞上下文
- ⭐ **出处必须可见**：文件名 + 行号 + 命中词 + 得分。
  没这些，检索就是个黑盒 —— 你没法判断它「找对了」还是「蒙对了」
- **IDF + 饱和词频**（BM25 的核心思想）：常见词不值钱；同一个词重复出现，收益递减
- **中文没有空格**：字符 bigram（「性能优化」→ 性能/能优/优化）是最土也最有效的零依赖解法
- **索引懒加载 + 指纹失效**：第一次检索才建（启动不变慢），文件变了自动重建
- ⭐ **关键词的天花板是「字面」**：同义词、近义表达**全部漏掉** —— 实验 3 亲手撞这堵墙
- **关能力要让它从工具清单消失**（v2 的老套路），而不是「调用时报错」
- 一个细节：`search_notes` 是**唯一在工具内部打印**的工具 ——
  黑盒工具的每个动作都要能被看见（铁律 7）

**留下了什么坑**

- ⭐ **不认同义词**：笔记写「性能优化」，你问「怎么让它更快」→ 0 命中（v5 最大的天花板）
- **没有重排**：按分数排，但「分数高」≠「真的相关」
- **召回数量是硬旋钮**：多了挤上下文，少了漏
- **只认 3 种文本格式**（`.md` / `.txt` / `.markdown`），PDF / Word 读不了
- **没有增量索引**：改一个文件就整个重建（小库无感，大库会痛）
- **它得先知道该查**：靠工具描述引导 —— 模型有不调用的自由

---

## ✅ 05b · 语义检索（embedding）（v6）

**解决什么问题**

v5 的关键词检索**认字不认意思**：

```
笔记里写「性能优化」  →  你问「怎么让它更快」  →  检索不到 ✗
```

这不是 bug，是这一类方法的物理上限。而用户不知道你库里是怎么写的 ——
所以这个坎必须跨过去。

**核心概念**

**把「字面匹配」换成「向量相似」**：把文字映射成一串数字（向量），
意思相近的文字，向量也相近。

```
关键词：找「字一样的」    →  只能碰运气
embedding：找「意思近的」  →  推荐 / 搜索 / 问答的通用底座
```

**流水线完全不变**（切分、塞上下文、出处全都一样）——
换掉的只是 `retrieval.py` 里 `search()` 那一段打分逻辑。
这也是 v5 把检索器单独拆成一个文件的原因。

**学到了什么**

- ⭐ **向量检索 = 按意思找**，治好了「换个说法就找不到」这个死穴
- **向量从哪来要抽成接口**（`Embedder`）—— 和 v3 的 `Summarizer` 同一个理由：
  真实实现要联网要花钱，测试里塞确定性替身，整条流水线就能离线测穿
- ⭐ **缓存是刚需**：embedding 按 token 计费。按「内容指纹」缓存之后，
  内容没变就零成本，变了也只重算那一块（v4 的「目录指纹」精确到了「每一块」）
- **缓存要按模型分区**：换模型 = 全部向量作废，混用不同模型的向量 = 一堆噪音
- **查询词不进缓存**：缓存是给「反复要用、总量有限」的东西准备的
- ⭐ **门槛是这一版唯一没法靠代码解决的问题**：0.5 只对「这个模型 + 这份资料」成立
- ⭐ **评估一个组件时，要把它单独拎出来测** —— 模型会自己**改写查询**
  （问「怎么让程序跑得更快」，它交给检索器的可能是「… 性能优化」），
  这让 v5 也可能命中，反而掩盖了检索器本身的差别
- 降级路径第三次出现（v3 压缩失败 / v4 坏文件 / v6 向量服务挂）：
  **主路径决定你有多好，降级路径决定你能不能用**

**实测**（干净对照：固定查询词、直接调检索器）

| 查询 | 关键词（v5）| 向量（v6）|
|---|---|---|
| 怎么让程序跑得更快 | **0 命中 ✗** | **1 命中（0.651）** ✓ |
| 这段代码太慢了怎么改善 | **0 命中 ✗** | **2 命中（0.621）** ✓ |
| 番茄需要浇多少水 | 0 命中 | 0 命中（0.362 < 门槛）✓ |

跨进程缓存实测：第 1 次「调用 2 次 · 3 段 · 44 tokens」→
第 2 次「**缓存命中 2 段（没花钱）** · 调用 1 次 · 1 段 · 5 tokens」

**留下了什么坑**

- ⭐ **门槛没有普适值**：换模型、换语料都得重新校准
- ⭐ **精确匹配未必更强**：「ERR_5002」这种无意义串，embedding 模型没见过，只能猜
- **规模**：现在是「每个查询跟所有向量算一遍」，几万块就得上专业向量库
- **改资料就要花钱**：缓存挡住了重复计算，但内容一变就得重算
- 它还是不会自己去查（靠工具描述引导）

---

## ✅ 05c · 混合检索（关键词 + 向量）（v7）

**解决什么问题**

v6 的实验 6 / 7 摆出了两个同样真实、方向相反的事实（干净对照）：

| 查询 | 关键词（v5）| 向量（v6）|
|---|---|---|
| 怎么让程序跑得更快 | **0 命中 ✗** | **1 命中（0.651）** ✓ |
| ERR_5002 | **1 段稳稳命中 ✓** | 0.556 —— 门槛 0.5→0.6 就死 ✗ |

两把尺子，盲区几乎不重叠：一个漏同义、一个漏精确；一个保守、一个冒进。

**核心概念**

**两个都跑，然后融合排名 —— RRF（倒数排名融合）。**

```
查询 ──┬─→ 关键词检索 ─→ 排名 A ─┐
       └─→ 向量检索   ─→ 排名 B ─┴─→ RRF 融合 ─→ 前 N 段 ─→ 塞进上下文
```

核心顿悟：**分数不可比，但名次是通用语** —— `score = Σ 1/(k + 名次)`。
被一边提名拿一份分，两边都提名拿两份，**共识是真金白银**。

三个想清楚了的设计决定：每一路按自己的老标准发言（命中规则不改，0 命中不硬塞）；
合并时按【内容】去重；每条结果都带两路名次（结果不对时，一眼看出是谁的锅）。

**学到了什么**

- ⭐ **RRF 只看名次、不看分数**：因为两边量纲完全不同（BM25 打分 vs 0~1 余弦），
  相加是「谁量大谁说话」而不是「谁说得对谁说话」——名次是唯一的通用语
- ⭐ **共识是真金白银**：双票（两边都提名）0.0328 > 单票 0.0164（k=60 时正好一倍）；
  名次平平的双边结果，能压过单边的「第一名」
- **k 是「共识的含金量」**：k=0 时单边霸权翻上来（A 反超 C）；k 越大越奖励共识
- **depth 是融合的隐藏门槛**：名单之外 = 没有名次 = 贡献 0 分
- ⭐ **降级又进化了一层**：v6 里「向量挂了退回关键词」是无奈；
  v7 里关键词本来就是并跑的一路 —— 退回去是「少一个帮手，基本盘还在」
- ⭐ **纯函数的价值**：`rrf_fuse`（名单进、排名出）让融合能被**手算核对**、
  被构造名单直接测穿 —— 离线断言 26 项，不花钱
- **组件评估继续绕开模型**（v6 老教训继续生效）：模型改写的搜索词会把
  「哪一路在救场」搅浑，所以对照实验全部固定输入、直调检索器

**实测**（干净对照：固定查询词，三模式并排）

| 查询 | 关键词 | 向量 | 混合（提名 k+v）|
|---|---|---|---|
| 性能优化 | 1 段 | 1 段（0.80）| 1 段（1+1）|
| 怎么让程序跑得更快 | **0 ✗** | 1 段（0.65）| **1 段（0+1）← 向量单独救回** |
| ERR_5002 | 1 段 | 1 段（0.56）| 1 段（1+1 双票 0.0328）|
| 番茄需要浇多少水 | 0 ✗ | 0 ✗（0.36）| **0 ✗（不会硬凑）** |

门槛 0.5 → 0.6（同一查询 ERR_5002）：双票 `0.0328` 腰斩为单票 `0.0164` ——
**但结果没丢**（关键词兜住）。对照 v6：同样的门槛下，那条会**彻底搜不到**。

**留下了什么坑**

- ⭐ **旋钮四件套**（门槛 / k / depth / top_k）：好结果依赖手工实验，
  没有系统的评估办法 —— 这是知识块 07 的领域
- **「改了参数到底变好还是变坏」没法回答**：目前全靠人肉看日志
- **成本**：每次检索多一份查询向量（约 5 tokens/次；块向量走缓存）
- 它还是不会自己去查（靠提示词 + 低温管着，模型说了算）
- ⭐ **一个顺序上的发现**：评估的第一步卡在「判定要程序化」上 ——
  「期望命中」「好 / 坏」现在全是自由文本 → 先需要 06（结构化输出）打底

---

## ⬅️ 06 · 结构化输出（下一版 · v8）

**解决什么问题**

v7 之后最急的其实是「评估」（把 k / depth / 门槛扫一遍）—— 但那是 07。
在它之前先过这一关：**评估的第一步就卡在「结构」上** ——
「期望命中」「好 / 坏」现在全是给人看的自由文本，连实验都是一叠人肉记录。
要让程序自动消费、自动判定，先得让**模型的输出**变成程序能可靠解析的结构。

这一版自己也埋着伏笔：融合报告（名次、票数、分数）在程序侧是个小结构
（`SearchHit.ranks`），而模型侧的输出还是自由文本 —— 下一版把这条路打通。

**核心概念**：JSON Schema 约束、校验、失败重试。

**起点**

1. 回顾 v1 起就在用的 tool calling —— 工具**参数**早就是 JSON Schema 了，
   这一版是把它用到「模型的输出」上
2. 想清楚：为什么「让模型保证输出格式」是一个**可靠性**问题，而不是提示词问题
3. 读 v7 README 的四个问题④：旋钮靠人肉调 → 评估需要它做前置

**预计会撞的墙**

| 墙 | 说明 |
|---|---|
| **模型不遵守 schema** | 说好的 JSON，它给你包一层 markdown、加一句解释 —— 怎么办？|
| **失败重试** | 校验不过后把错误回灌让它改 —— 改几次？什么时候放弃？|
| **嵌套结构** | 越复杂的 schema 越容易错 |
| **流式下怎么解析半截 JSON** | 字还没吐完，JSON 是非法的 |
| 什么时候**不该**用 | 自由对话不该被塞进 JSON —— 分清「给人看」和「给程序吃」|

**验证思路**

- **离线**：schema 校验器 + 重试用例（喂假的坏输出，断言重试与放弃逻辑）
- **真实**：固定任务让模型输出结构化结果，统计「第一次就合规」的比例
- **反例**：故意写一个模糊的 schema，看失败率怎么变

**做完之后，这张表要更新成**：06 标 ✅，07（评估与可观测）变成下一版。

---

## ⬜ 07 · 评估与可观测

**解决什么问题**：改了 prompt 或工具描述，**你不知道是变好了还是变坏了**。

**核心概念**：trace（每一步都记下来）+ eval 集（一批固定测试用例 + 期望结果）。

**预计会撞的墙**：LLM 输出没有唯一正确答案，怎么判定「对」？
这是最容易被跳过、但**回报最高**的一块 —— 没有它，前面所有优化都是瞎猜。

---

## ⬜ 08 · 规划与任务拆解

**解决什么问题**：复杂任务，它只会「走一步看一步」，容易迷路。

**核心概念**：Plan-and-Execute（先出计划再执行）、ReAct（边想边做）、任务分解。

**预计会撞的墙**：计划做出来但执行偏了怎么办？计划要不要给用户确认？

---

## ⬜ 09 · 反思与自我修正

**解决什么问题**：它做错了不会自我批评，错了就一直错下去。

**核心概念**：Reflexion —— 让模型检查自己的输出并改进。

**预计要撞的墙**：自我批评经常是无效的（它觉得自己对）；反思也要花 token。

---

## ⬜ 10 · 流式与人机协同

**解决什么问题**：等半天没反应；中途没法干预。

**核心概念**：streaming 输出、中断、human-in-the-loop（危险操作前要确认）。

**预计会撞的墙**：流式下 `tool_calls` 是分片到达的，要自己拼装；
流式和剪枝的交互也很微妙。

---

## ⬜ 11 · 多 Agent 协作

**解决什么问题**：一个 Agent 兼太多角色（规划、执行、检查），提示词互相打架。

**核心概念**：角色分工、handoff（交接）、编排。

**⚠️ 注意**：这一块最容易被过度使用。**先确认单个 Agent 真的做不好**再上多 Agent。

---

## ⬜ 12 · MCP 工具生态

**解决什么问题**：每个工具都要手写，接不了外部生态。

**核心概念**：MCP（Model Context Protocol）—— 用标准协议接入别人写好的工具。

---

## ⬜ 13 · 框架化（LangChain / LangGraph）

**解决什么问题**：到这里，手写的主循环通常会开始失控。

**⚠️ 什么时候才该上框架 —— 看信号，别看版本号**

出现下面任意一条，就说明到时候了：

- 主循环里出现 **3 个以上 `if/else`** 在决定「接下来做什么」
- 想加「这条失败就换条路走」，发现要在循环里再嵌一层
- 多 Agent 之间的消息传递，开始需要**手写状态机**
- 想实现「暂停 → 等用户确认 → 继续」，发现自己要管一大堆状态

**这时 LangGraph 的价值才会显现** —— 它给的就是「状态机 + 检查点 + 人机协同」。

> **在没痛过之前上框架，你只会觉得它又重又绕。**
> 这也是为什么它排在最后几块，而不是第一块。

---

## ⬜ 14 · 成本、并发与部署

**解决什么问题**：贵、慢、上不了线。

**核心概念**：prompt 缓存、并发、限流、重试、成本监控、部署形态。

---

# 速查：每块对应哪个「痛」

| 你感受到的痛 | 该学哪块 |
|---|---|
| 「它究竟是什么？」 | 01 |
| 「怎么越聊越贵，还会报错」 | 02 |
| 「它怎么把我刚说的事忘了」 | 03 |
| 「重启就什么都不记得」 | 04 |
| 「它不知道我的资料」 | 05 |
| 「输出没法程序化消费」 | 06 |
| 「改了 prompt 不知道有没有变好」 | 07 |
| 「复杂任务它做不明白」 | 08 |
| 「错了不会自己改」 | 09 |
| 「等半天没反应」 | 10 |
| 「一个提示词里塞太多角色」 | 11 |
| 「工具全要手写」 | 12 |
| 「主循环快改成意大利面了」 | 13 |
| 「太贵/太慢/上不了线」 | 14 |
