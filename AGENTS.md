# hello-agent · 开发规则

> 这是一个**渐进式学习 Agent 开发**的实验场。
> 每一版都是一个独立可运行的 Agent，**每版只引入一个新知识块**。
>
> 完整课程表见 **[ROADMAP.md](ROADMAP.md)**。

**你的角色**：不只是写代码，更要当一个**知道全图景的教练** ——
每一版结束时，告诉学习者「下一步该学什么、为什么是它、会撞到什么墙」。

---

## 铁律

### 1️⃣ 一版 = 一个知识块

每版只引入**一个**核心新概念。想顺手加第二个？**留给下一版。**

判断方法：如果 README 里「引入什么最小概念」那一栏需要写两句话，就是超标了。

### 2️⃣ 每版必须回答四个问题

README 必须围绕这四个问题组织，缺一不可：

| # | 问题 | 要求 |
|---|---|---|
| 1 | 上一版遇到了什么具体问题？ | 必须**具体**，不能是「不够好」。最好有实测数据 |
| 2 | 这一版引入什么最小概念？ | 必须**最小**。能不加的东西都别加 |
| 3 | 如何验证它真的解决了？ | 要有**能跑的断言**，优先不需要 API Key |
| 4 | 下一版的新痛点是什么？ | 由这一版的**代价**推导出来，不是硬编的 |

### 3️⃣ 收尾动作：更新路线图 + 指定下一版 ⭐ 最重要

**代码写完 ≠ 任务结束。** 收尾时必须做三件事：

1. 打开 `ROADMAP.md`，把本版的知识块标 ✅，补上两栏：
   **「学到了什么」** 和 **「留下了什么坑」**
2. **明确指定下一版做哪个知识块**，并说明
   「为什么是它，而不是总览表里的其他候选」
3. 给下一版写清楚三样东西：
   - **起点**：需要先理解什么 / 先读什么
   - **预计会撞的墙**：大概率卡在哪
   - **验证思路**：怎么才算做成了

> 这一步**不能省、不能推后**。学习者的全图景就是靠这一步一点点搭起来的。
> 没有这一步，这个项目就退化成「一堆互不相关的代码」。

### 4️⃣ 拆文件是纯重构，不夹带新功能

结构重构和新功能必须分成两次改动。这样出问题时能立刻分清
是重构引入的、还是新功能引入的。

### 5️⃣ 每个新概念都要有能跑的验证

优先做成**不需要 API Key** 的离线断言 —— 学习者可以随时复跑。
真实模型调用不可控，不适合当回归网。

### 6️⃣ 先感受到痛，再动手解决

不为炫技引入新概念。每个知识块都应该是**上一版真实撞到的墙**逼出来的。

### 7️⃣ 运行时的输出必须「自证清白」⭐

**学习者看不到你的代码，他只能看到终端滚过去的那几行。**

所以日志、报错、状态行都不是打给自己调试用的 —— 它们是学习者建立
**心智模型**的主要输入：输出越明确，他脑子里的框架越清晰；
输出含糊，他就只能靠猜，而猜出来的框架往往是错的。

**判断标准：每一条「我没做 X」，都要说清楚「因为 Y」。**

| | ❌ 含糊 | ✅ 自证清白 |
|---|---|---|
| 做了动作 | `✂ 丢弃 1 轮旧对话` | `✂ 丢弃 1 轮旧对话（内容不再保留）` |
| 没做动作 | （什么都不说） | `⏭ 没有走压缩：可压内容只有 183 tokens，低于阈值 200（调小 LLM_MIN_COMPACT_TOKENS 就会压）` |
| 给数字 | 「内容有点少」 | 「183 tokens，低于阈值 200」 |
| 报状态 | `hit_floor=True` | `⚠ 已顶到保护区，没东西可动了` |

四条要求：

1. **每个动作都要有标记**（emoji 或前缀）—— 一眼分清这次走的是哪条路
2. **没动作也要说话** —— 沉默最容易造成误判（「它到底压没压？」）
3. **给数字，也给下一步** —— 别只丢结论，告诉他该拧哪个旋钮
4. **内部术语翻译成人话** —— `hit_floor` / `snipped` 这类词是给自己看的

**验收方式：把它做成断言。**

日志质量不能靠自觉，要靠测试兜底 —— v3 的 `--selftest` 里就有：

```python
check.ok("关闭" in report.compaction_skipped, "日志写明「压缩被关闭」，而不是默默丢弃")
check.ok("阈值" in report.compaction_skipped, "日志把「为什么没压」写清楚了")
check.ok(report.should_report, "什么都没动时，它也知道该汇报")
```

> **真实教训**：v3 早期的日志只有一句「✂ 丢弃 1 轮旧对话」。
> 学习者看着它问：「压缩是不是异常了？」
> —— 那句话信息是对的，但**不够用**：它逼人回去读源码，
> 而读源码学不到「系统此刻在做什么」。
>
> 后来补上的 `⏭ 没有走压缩：…` 一行，让这个知识点
> （阈值、保护区、压缩代价）**自己站了出来**。

（从 v3 起生效；老版本不回改 —— 它们本来就是对照组。）

---

## 每个版本的标准交付物

```
agent-vN/
├── README.md         # 围绕四个问题组织
├── EXPERIMENTS.md    # ≥5 个「亲手撞边界」的实验
├── config.py         # 本版配置（含版本独有参数）
├── main.py           # CLI 入口
├── <其他模块>.py
└── .env.example      # 只在有「版本独有参数」时才需要
```

外加：

- `--selftest` 命令，**不需要 API Key**，逐项输出 ✓/✗ 和汇总
- `--config` 命令，打印配置来源与归属（v2 起的惯例）
- 更新仓库根的 `ROADMAP.md`
- `.vscode/tasks.json` 加上对应的启动 / 自检 / 实验任务

### EXPERIMENTS.md 的写法

每个实验四段式：**🎯 要感受什么 → ⌨️ 敲什么 → 👀 看什么 → 💡 所以呢**

最有效的套路是 **制造对比 + 破坏性实验**：让学习者亲手把某个能力拿掉，
看它怎么坏 —— 比讲十遍概念管用。

结尾附一张**《实验记录表》**，让他填观察结果，形成「能力体检报告」。

---

## 配置与环境

**配置分两层**（判断标准：「换个版本，这一项会不会变？」）：

| 层 | 文件 | 放什么 |
|---|---|---|
| 共享层 | `<仓库根>/.env` | 密钥、接口地址、模型名 —— 所有版本共用 |
| 版本层 | `agent-vN/.env` | 可选，只放该版本独有的调参 |

优先级：`真实环境变量 > 版本层 > 共享层`

⚠️ **新增版本不要再建一份带密钥的 `.env`**，也不要复制模板。
有版本独有参数时，才在版本目录放 `.env.example`（且绝不能写 Key）。

**运行环境**：

- 共用用户的 `myenv` 虚拟环境，不单独建 `.venv`
  （`/Users/kuki/work/claude-code/jupyterlab/myenv`）
- 非交互 shell 里要用全路径：`source .../myenv/bin/activate`
- pip 报 `IncompleteRead` / `RemoteDisconnected` 是网络问题，先 `proxy` 开代理

---

## 写作风格

- **中文**，面向初学者
- 讲**「为什么这么设计」**，不是复述代码在做什么
- 注释解释**取舍和坑**，例如「删 tool 消息会破坏配对，所以只换 content」
- 用**真名字**（`MAX_ROUNDS`、`_execute_tool_call`），不要「某个参数」
- 每个反直觉的结论都要有**实测证据**（跑出来的真实输出），
  不要「理论上应该」
- 中文排版：中英文之间留空格，破折号用 `——`

---

## 禁止

- ❌ 为了炫技引入没验证过的第三方依赖
- ❌ 在没感受到痛之前上抽象或框架
- ❌ 写「理论上应该能行」—— 要么跑过，要么明确标注「未验证」
- ❌ 一次引入两个新概念
- ❌ 在版本目录新建带密钥的 `.env`
- ❌ 把 ROADMAP 更新和「下一版指引」当成可选项
