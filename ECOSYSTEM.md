# 社区方案速览

> 这份文档回答一个问题：**「我正在学的这一块，外面的人是怎么做的？」**
>
> **⚠️ 三条使用须知**
>
> 1. 它不是教程，是**地图** —— 告诉你外面有什么，**不要求你现在就去用**
> 2. 信息来自我的知识范围，这类东西变化极快 —— **用之前请以官方文档为准**
> 3. 每节都标了对应的知识块，方便你学完某一版之后回来对照

> **💡 最重要的一句话**：
> 不要因为「外面有现成方案」就跳过手写。
> 你手写过的每一行，都是将来**读懂这些工具取舍**的前提 ——
> 没痛过的人打开 LangGraph，只会觉得它又重又绕。

---

## 一张对照表：你的每一版，社区里都有身影

| 你亲手做的 | 社区里的同类 | 关键差别 |
|---|---|---|
| v1 最小闭环 | 几乎所有 Agent 框架的 hello world | 你多了一个「工具描述就是提示词」的体感 |
| v2 两级剪枝 | LangGraph `trim_messages`、各家产品的「历史截断」 | 你的 look-ahead 算账和 `hit_floor` 如实标记，比多数实现更诚实 |
| v3 摘要压缩 | Claude Code 的 `/compact`、LangChain `ConversationSummaryBufferMemory` | 结构几乎一样；你多了「压缩成本」的显式记账 |
| v4 会话存盘 | LangGraph checkpointer、ChatGPT/Claude 的会话历史 | 它们存在数据库里，你存在 JSON 里 —— **思路完全相同** |
| v5 本地检索 | LlamaIndex、向量库生态 | 你大概会先用最土的办法，这是对的 |

**结论先给你**：你走过的这条路，是社区方案的**同一条路**，
只是每一步都小得多、清楚得多。这就是这个项目的意义。

---

## 03 · 上下文压缩

**对应你的**：v3 · 上下文压缩

### 社区的共同思路（重要）

几乎所有认真做上下文管理的产品，最后都收敛到**同一个结构**：

```
┌─ 长期：早前对话的摘要   ← 省 token，保关键信息
└─ 短期：最近 N 轮原文     ← 精确，但贵
```

**没有人用「纯摘要」**（信息丢太多），**也没人只做「截断」**（丢得太干脆）。
你的 v3 正是这个结构 —— 这不是巧合，这是这个问题的自然解。

### 具体方案

**① Claude Code 的 `/compact`**（Anthropic 的 CLI 工具）

- 手动敲 `/compact`，它会读完整段对话、生成摘要、然后继续
- 也会在上下文快满时**自动压缩**
- 它把摘要放回上下文的方式，和我们 v3 的「一条摘要 system 消息」几乎一样

> 💡 有意思的对照：`/compact` 是**手动**的，而 v3 是**自动**的。
> 手动的好处是「用户知道什么时候该压」；自动的好处是不用管。
> 真实产品往往两个都做。

**② LangChain 的 Memory 家族**（这一组特别适合对照学习）

| 类 | 行为 | 相当于你的 |
|---|---|---|
| `ConversationBufferMemory` | 全存，不删 | v1（会爆） |
| `ConversationBufferWindowMemory` | 只留最近 k 轮 | 纯截断（比 v2 粗糙） |
| `ConversationSummaryMemory` | 全压成摘要 | v3 但没有「保护区」 |
| `ConversationSummaryBufferMemory` | **最近 N tokens 原文 + 更早的压成摘要** | ⭐ **和 v3 基本同构** |

看最后一行 —— **社区给这个东西起的名字，就是你 v3 在做的事**：
「Summary + Buffer」，摘要 + 保留区。

**③ LangGraph 的 `trim_messages`**

按 token 数裁剪消息，可以配置「保留 system / 保留最近多少 token / 裁剪策略」。
它更偏「机械裁剪」，不如摘要那条线聪明 —— 但足够快、足够可预测。

**④ MemGPT / Letta**

一个非常有意思的思路：**别把上下文当缓存，把它当「内存」，把外部存储当「磁盘」。**

- 上下文窗口 = RAM（快、小）
- 外部存储 = 磁盘（慢、大）
- 模型自己决定：什么时候把什么**换出**（page out）到磁盘、什么时候**换入**（page in）

它把这个过程叫「记忆分页」，灵感直接来自操作系统。
v3 的压缩是「单向的、不可逆的」；MemGPT 追求的是**可换入换出** ——
信息没有真的丢，只是不在「内存」里。

> 💡 这个思路在 05（RAG）里会再次出现：
> **「上下文里放不下的，放到外面，需要时再取回来」** ——
> 压缩和检索，本质上是同一个问题的两种解法。

### 和你的 v3 对照

| | 你的 v3 | 社区方案 |
|---|---|---|
| 结构 | 摘要 + 保护区 | 完全一样 |
| 触发 | 超预算自动 | 手动 / 自动都有 |
| 摘要指令 | 结构化模板（目标/关键信息/事实/待办） | 各家不同，但都强调「保留关键事实」 |
| 失败兜底 | 回退到「丢弃」 | 通常也有 fallback（截断） |
| **成本可见** | ⭐ 每次压缩都报「花了多少 token」 | 少见 —— 这是你做得比它们**更透明**的地方 |
| 可离线验证 | ⭐ 45 项断言，含「暗号还在吗」 | 少见如此 |

---

## 04 · 持久化记忆

**对应你的**：v4 · 持久化记忆

社区方案分成**两派**，理解这个分岔比记住产品名重要得多。

### A 派：会话级持久化（把「这场对话」存下来）

**这就是你的 v4。**

| 方案 | 怎么做的 |
|---|---|
| **LangGraph checkpointer** | 最有代表性的实现：`MemorySaver`（内存）/ `SqliteSaver` / `PostgresSaver`（持久化）。用 `thread_id` 区分不同会话；**每一步**都存一个 checkpoint（不是每轮）|
| ChatGPT / Claude / 各家 App | 左侧那个「历史会话」列表 —— 用户能看见的持久化 |
| 你自己手写的 JSON | 最简单、最透明的一种 |

**LangGraph 比 v4 多出来的两个能力**（值得知道，但你现在不需要）：

1. **时间旅行**：因为每步都存，可以「回到第 5 步重新走」
2. **断点恢复**：程序崩了，能从最后一个 checkpoint 继续 ——
   这正是「人机协同」（暂停→等确认→继续）的技术基础

> 💡 你的 v4 是「每轮存」，LangGraph 是「每步存」。
> 差别就是：崩溃时你会丢「半轮」，它不会。

### B 派：记忆层（把「值得记住的事实」抽出来单独管）

**这一派做的事，你的 v4 没做** —— 它不管「会话」，它管「你这个人」。

| 方案 | 核心思路 |
|---|---|
| **Mem0** | 从对话里**自动抽取**值得长期记住的东西（偏好、事实、约束），存进向量库 + 图，需要时按相关度召回。主打「比把历史全塞进 prompt 更省、更准」 |
| **Letta（原 MemGPT）** | Agent 有一块「核心记忆」（永远在上下文里）+ 「归档记忆」（按需检索）。它自己编辑这些记忆 |
| **Zep / Graphiti** | 时序知识图谱 —— 记忆带时间维度，「什么时候知道的」也是信息 |
| **ChatGPT 的 Memory** | 产品化版本：记住你的偏好，跨会话生效 |

**关键区别一句话**：

```
A 派：记住「我们聊过什么」        → 事件
B 派：记住「你是个什么样的人」    → 事实 / 偏好
```

**还有第三种：文件式项目记忆**

你可能没注意到 —— **这个仓库里的 `AGENTS.md`，就是社区里「项目记忆」的一种实践**：

- `AGENTS.md` / `CLAUDE.md`：把项目的约定写成文件，每次注入上下文
- 它既不是「会话」（不随对话变化），也不是「用户偏好」（是项目级的）
- Cursor、Claude Code 等工具都原生支持这种文件

> 💡 所以你已经**在用一个记忆方案了** —— 只是它太自然，你没把它当"方案"。

### 和你的 v4 对照

| | 你的 v4 | 社区 A 派 | 社区 B 派 |
|---|---|---|---|
| 存什么 | 整场会话（消息+摘要）| 状态快照（每步）| 抽取出来的事实 |
| 存哪 | JSON 文件 | SQLite / Postgres | 向量库 / 图数据库 |
| 何时存 | 每轮 | 每步 | 异步抽取 |
| 恢复后 | 接着聊 | 接着跑（含工具状态）| 用记忆增强回答 |
| 透明度 | ⭐ 文件就是 JSON，`cat` 就能看 | 要查数据库 | 存在服务里，看不见 |

---

## 05 · 本地检索（RAG）

**对应你的**：v5（基础版：关键词检索）+ 05b（语义检索，下一版）

RAG 是社区里**最成熟、也最卷**的一块。好消息是：它的骨架很清晰。

### 骨架：一条流水线，五个环节

```
你的资料
  │  ① 切分 chunking
  ▼
 分成小块 ──→ ② 嵌入 embedding ──→ ③ 存进向量库
                                      │
                                      ▼
用户提问 ──→ 嵌入 ──→ ④ 召回 retrieve ──→ ⑤ 重排 rerank ──→ 塞进上下文 → 模型回答
```

**每一个环节都有成熟的库，也都有坑。** 逐段说：

**① 切分**

- 主流做法：**递归字符切分**（先按段落、再按句子、再按字符），
  而不是机械地「每 500 字一刀」
- 关键权衡：**切太碎 → 丢上下文；切太大 → 召回不准、占 token**
- 工具有 `RecursiveCharacterTextSplitter`（LangChain）这类现成的

**② 嵌入**

- 云端：OpenAI `text-embedding-3-*`
- 本地/中文友好：**BGE**（智源）、**M3E** 这一类开源模型
- 注意：**换 embedding 模型 = 全部重新算一遍**（这一点你迟早会撞上）

**③ 存**

| 向量库 | 定位 |
|---|---|
| **FAISS** | 本地、快，但不管持久化（就是一堆文件）|
| **Chroma** | 轻量、上手快，教学/原型最常用 |
| **Qdrant / Weaviate / Milvus** | 生产级，能扩、能过滤 |
| **pgvector** | 如果你已经有 Postgres —— 加个扩展就行，不引入新系统 |
| **LanceDB / sqlite-vec** | 嵌入式，适合「不想多跑一个服务」|

> 💡 `sqlite-vec` 值得留意：你在 v4 已经用了文件（JSON），
> 如果不想引入「一个数据库服务」，这类嵌入式方案是最自然的一步。

**④ 召回（检索）**

- **向量检索**：语义相似 —— 「我想吃苹果」能找到「水果推荐」，但找不到精确的型号、编号
- **关键词检索（BM25）**：精确 —— 「X1000 型」这种必须命中的东西，靠它
- **混合检索**：两个都跑，用 **RRF（倒数排名融合）** 之类的办法合并结果 ← 主流实践
- 只靠向量的产品，通常在「精确匹配」上翻车；**别只用一种**

**⑤ 重排（rerank）**

- 召回 20 条 → 用一个更贵、更准的模型（cross-encoder / `bge-reranker` / Cohere Rerank）
  重新排序 → 取前 3 条
- 效果通常比「把 20 条全塞进去」好得多，而且**省 token**（又回到 v2 的预算）

### 框架层

| 框架 | 特点 |
|---|---|
| **LlamaIndex** | 专为「索引 + 检索」而生，RAG 的花样它最全 |
| **LangChain** | 什么都有，RAG 只是其中一块 |
| 手写 | 你现在这样 —— **看得见每一步在干什么** |

### 和你的 v5 / v6 对照

| | 你的实现 | 社区主流 |
|---|---|---|
| 切分 | 按 Markdown 标题 + 段落 | 递归字符切分 / 语义切分 |
| 召回 | **关键词打分**（v5）→ **向量检索**（v6）| 两者混合（下一版 05c 追的就是这个）|
| 中文分词 | 字符 bigram（零依赖）| 中文场景多用 jieba 这类分词器 |
| 向量模型 | 远程 API（`bge-m3`）| 远程 API / 本地模型都有 |
| 向量缓存 | ⭐ 按「内容指纹」缓存，**变了只重算那一块** | 向量库存盘；但这种细粒度失效未必有 |
| 相关性门槛 | `LLM_MIN_SIMILARITY`（自己校准的）| 一般也有 score 阈值 |
| 重排 | 无 | cross-encoder / `bge-reranker` / Cohere Rerank |
| 出处 | ⭐ 每段都带「文件:行号 + 命中词/相似度」| 通常只标「来自哪个文件」，很少精确到行 |
| 降级 | ⭐ 向量挂了 → 自动退回关键词，并说明原因 | 少见（多数实现直接报错）|
| 可测性 | ⭐ 101 项断言全离线，不联网不花钱 | 通常靠集成测试或人工看 |

**这一版补上了什么、还缺什么：**

1. ~~embedding~~ → **补上了**（v6）。而且印证了 v5 那句话：
   换掉 `search()` 一个函数就够了，切分 / 索引 / 出处全都没动
2. **混合检索** → 缺，而且是**下一版的正题**。社区主流不是「二选一」，
   而是「关键词保精确、向量保语义，RRF 融合」
3. **重排（rerank）** → 缺。生产系统通常「粗召回 20 条 → 重排取前 3 条」
4. **向量库** → 缺。你用**内存 + JSON 缓存**：几百块无感，
   几万块就得上 FAISS / Qdrant / pgvector 那类专业存储

> 对照的价值在于：**你现在能读懂「为什么它们要加那些东西」。**
> 因为你亲手撞过「召回太多挤爆上下文」（v5 实验 5）、
> 「换个说法就找不到」（v5 实验 3 → v6 治好了）、
> 「不设门槛就会硬塞无关内容」（v6 实验 3）——
> 这几件事，光看文档是感受不到的。

### 两个进阶方向（知道有这回事就行）

- **GraphRAG**（微软开源）：先把资料建成知识图谱，再检索 ——
  解决「这个问题的答案分散在 50 个段落里」这类向量检索很吃力的情况
- **Agentic RAG**：不让 RAG 只查一次，而是**让 Agent 自己决定**查不查、查几次、
  换个关键词再查 —— 这时候你会发现：**工具调用（v1）和检索（v5）合流了**

---

## 06 ~ 14 · 先扫一眼（以后再来）

| 块 | 社区里比较有代表性的东西 | 一句话 |
|---|---|---|
| 06 结构化输出 | OpenAI 的 Structured Outputs（JSON Schema 约束）、Instructor、Pydantic | 让模型「只能按这个格式说话」，而不是「求它按格式说」 |
| 07 评估与可观测 | LangSmith、LangFuse、Arize Phoenix、Ragas（RAG 专用） | trace（每步留痕）+ eval 集（固定用例）；**最容易被跳过但回报最高** |
| 08 规划与任务拆解 | ReAct（论文）、Plan-and-Execute、LangGraph | 「边想边做」还是「先出计划」—— 这是两种不同的哲学 |
| 09 反思与自我修正 | Reflexion、Self-Refine（都是论文/模式，不是产品）| 让模型检查自己 —— 经常无效，但有奇效的时候 |
| 10 流式与人机协同 | 各家 streaming API、LangGraph 的 `interrupt`、Vercel AI SDK | 流式的难点在 `tool_calls` 是分片到达的，要自己拼 |
| 11 多 Agent 协作 | AutoGen、CrewAI、OpenAI Agents SDK | **最容易过度使用的一块**，先确认单个 Agent 真的不行 |
| 12 MCP 工具生态 | Model Context Protocol（Anthropic 发起）+ 大量现成 server | 工具不用自己写了 —— 但「工具描述写不好」这个基本功照样要练 |
| 13 框架化 | LangChain / LangGraph / LlamaIndex / Pydantic AI / DSPy | 上不上框架**看信号，不看版本号**（ROADMAP 里列了四个信号）|
| 14 成本与部署 | prompt caching、vLLM、Ollama、批处理 API | 到这一步，你才会真正在乎「缓存命中率」这种词 |

---

## 最后：三个「不要」

**1. 不要因为「社区有」就换掉你手写的版本。**

你的 `context.py` 有 573 行，LangGraph 的 `trim_messages` 可能有几千行 ——
但那几千行里，大部分是在解决**你现在还遇不到的**问题（并发、多租户、序列化兼容……）。
用你现在的需求去换，只会换来一个「看不懂、调不动」的黑盒。

**2. 不要因为「社区没有」就不做。**

你能列出十种把你现在这版做错的方式，说明你比大多数使用者更懂它。
社区方案是**参考答案**，不是**标准答案**。

**3. 不要一次引入两个新东西。**

这条不用我提醒了 —— 你已经按照它走了四版。

---

> **本页写于 2026-09-17**，内容基于我的知识范围。
> 这个领域的产品迭代极快（半年就能换一批），
> 这份文档的价值在于**「有哪些类别的解法」**，而不是「某产品现在支持什么功能」。
> 具体细节请查官方文档。
