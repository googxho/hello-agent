# hello-agent v3 的依赖，和 v1/v2 完全一致 ——
# 上下文压缩是「多调一次模型 + 一段提示词」实现的，没有引入任何新依赖。
#
#   openai          —— 官方 SDK，用它的 OpenAI 兼容客户端调模型
#   python-dotenv   —— 把 .env 里的配置读进环境变量
#   rich            —— 终端高亮，尤其是把压缩 / 剪枝过程显示出来
openai>=1.30.0
python-dotenv>=1.0.0
rich>=13.0.0
